# ملاحظات وفجوات (Issues)

لا تُدرج هنا إلا ملاحظات مسنودة بأدلة أو بتصريح صريح من جولة التحقق. **لا أخطاء مُختلقة.**

## حل تناقض الاسترداد النقدي (مهم)

ادعاء جولة DEMO-GUIDE-20260924 «لا يوجد API استرداد نقدي للعميل» كان **خاطئاً** — ليس انحداراً في الإنتاج.

| البند      | الحقيقة الموثّقة                                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| السبب      | سكربت قديم `scripts/acceptance/data-tour.mjs` لم يستدعِ `/financial-transactions/refunds`                                                                           |
| الإنتاج    | SHA = HEAD = `origin/main` = **f52828c17696ae9e0e65fe0aa6500b6b5fb58b64** (نفس نشر Vercel)                                                                          |
| متى أُضيف  | commit **acfe68a** (سلف لـ f52828c؛ **db9ae9e** أيضاً سلف وليس فرعاً مختلفاً)                                                                                       |
| المسار API | `/financial-transactions/refunds` (+ `/confirmed`، `/refundable/:id`)                                                                                               |
| الواجهة    | إجراء مرتجع المبيعات **«رد مبلغ»**؛ قائمة `/sales/payments?view=refunds`؛ تفاصيل `/sales/refunds/:id`                                                               |
| سجلات      | CRF-2026-000001 (CONFIRMED، 450، ضد SR-2026-000014)؛ CRF-2026-000002 (CONFIRMED، 450، ضد SR-2026-000015) → بعد الرد `refundableAmount=0` و`customerCreditBalance=0` |

التفاصيل: [evidence/VERSION-REFUND-RESOLUTION.md](./evidence/VERSION-REFUND-RESOLUTION.md).

---

## ملاحظات من جولة DEMO-GUIDE-20260924 (بعد التصحيح)

### 1) ~~لا يوجد استرداد نقدي~~ → **مُبطَل**

استُبدل بالفقرة أعلاه. الرصيد الدائن بعد المرتجع كان حالة مؤقتة قبل صرف الرد النقدي، لا غياب الميزة.

### 2) تشغيلات دفعية عالمية لم تُنفَّذ عمداً

- **الدليل:** F6 `depreciationRun` و `prepaidRecognition` = SKIPPED ما لم يُضبط `ALLOW_GLOBAL_RUNS=1`.
- **الحالة الحالية:** `ALLOW_GLOBAL_RUNS` ما زال **OFF** — دفعة الإهلاك/إثبات المقدمات لم تُشغَّل عالمياً.
- **الحكم:** ليس فشلاً؛ فجوة تحقق لدفعة الإهلاك/الإثبات الدوري.

### 3) إنشاء عميل محتمل من الواجهة — **PASS**

- **الدليل:** LD-2026-000081 عبر حوار الإضافة (اسم + مصر + هاتف؛ القيمة تُثبَّت في النموذج عند blur لحقل الهاتف).
- لقطات: `06-lead-create-filled-ar.png` · `07-lead-detail-LD081-ar.png`.

### 3b) طباعة التقارير — **FAIL** على f52828c → إصلاح معلّق بالنشر

- **العَرَض:** `/print/list?job=…` يعرض «انتهت صلاحية مهمة الطباعة» فوراً في متصفح الأتمتة (وغالباً عند فتح التبويب بدون وراثة `sessionStorage`).
- **السبب:** `print-bridge` كان يعتمد على وراثة `sessionStorage` من `window.open`.
- **الإصلاح:** التحويل إلى `localStorage` + TTL في `apps/web/src/lib/print-bridge.ts` — يحتاج نشراً ثم إعادة اختبار طباعة ميزان المراجعة.

## فجوات تغطية (ليست بالضرورة عيوباً)

| الفجوة                                                      | الحالة                                               |
| ----------------------------------------------------------- | ---------------------------------------------------- |
| حسابات QA جديدة: `qa-purchasing` / `qa-investors` / `qa-hr` | في `ensure-qa-users.ts`؛ **بانتظار نشر/بذر**         |
| صلاحيات `sales.refunds.*` لـ finance                        | في السكربت؛ PENDING إعادة بذر                        |
| استيراد طلبات/شحن                                           | تحميل صفحة فقط                                       |
| Landed cost، جرد فعلي، بنوك، مشاريع، قواعد تكلفة            | تحميل صفحة أو غير مختبر                              |
| معظم فئات التقارير غير المالية                              | SHELL «قريباً»                                       |
| معظم صفحات الإعدادات                                        | SHELL «قريباً»                                       |
| منطق HR العميق                                              | NOT TESTED                                           |
| مستثمرون API                                                | **PASS** 82/82 (GAP) — متصفح شخصية BLOCKED حتى البذر |
| إعدادات: تعديل قابل للعكس                                   | NOT TESTED (تجنب تغيير حسابات افتراضية حية)          |
| إهلاك/مقدمات دفعية عالمية                                   | NOT TESTED — `ALLOW_GLOBAL_RUNS` OFF                 |

### 4) Browser-tour / gap: حوار الاسترداد — **PASS**

- جولة DEMO الأصلية كانت BLOCKED لنقص معرفات.
- **GAP:** CRF-2026-000003 عبر الواجهة + JV-2026-000270 + refundable→0 + كشف closing 0.

### 5) صلاحيات الأدوار (واجهة)

- **الدليل:** `evidence/role-ui-nav.json` (2026-09-24).
- **النتيجة:** `qa-shipping` يرى الشحن ولا يرى المالية/الإعدادات؛ `qa-finance` يرى المالية؛ `qa-sales-agent` يرى المبيعات وCRM ولا يرى الإعدادات — **PASS**.
- أدوار purchasing/investors/hr الجديدة: لم تُتحقق بعد على الإنتاج (بانتظار البذر).

## ما نجح ويستحق التذكير (ليس عيوباً)

- تكرار Confirm & Post آمن (`alreadyPosted`).
- رفض التحويل بمبلغ 0 مع الإبقاء على العميل المحتمل.
- رفض دفعة على مستند مسدَّد بالكامل.
- Browser sweep: كل مسارات التنقل HTTP 200 بدون خطأ واجهة (129 PASS).
- حماية القيود النظامية من التعديل/الإعادة لمسودة.
- رفض المرتجع الزائد.
- استقلال الدفع عن الشحن.
- تقارير TB/GL/BS/P&L/CF وكشوف الشركاء متسقة في F7.
- استرداد نقدي عميل عبر API + سجلات CRF مؤكدة على الإنتاج.

## فشل فحوصات البيانات

`failedChecks: []` — لا FAIL مسجّل في تقرير DEMO-GUIDE-20260924.
