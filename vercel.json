{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "DATABASE_URL=$POSTGRES_URL_NON_POOLING pnpm --filter api exec prisma migrate deploy && pnpm --filter api exec prisma generate && (DATABASE_URL=$POSTGRES_URL_NON_POOLING pnpm --filter api exec ts-node prisma/provision-permissions.ts || echo 'PROVISION_PERMISSIONS_STEP_FAILED_NONFATAL') && pnpm --filter web run build",
  "outputDirectory": "apps/web/.next",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [{ "source": "/api/:path*", "destination": "/api/[...path]" }]
}
