/**
 * Placeholder identity — no Authentication module exists yet (see TODO.md).
 * Deliberately generic, not a fabricated real person, so it reads
 * unambiguously as "not signed in yet" rather than real account data.
 */
export const placeholderUser = {
  name: "Guest User",
  email: "Not signed in",
  initials: "GU",
} as const;
